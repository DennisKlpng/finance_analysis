# finance_analysis
Pet project for spending tracking outside of Excel

To use: simply build and run the application. It starts a local webserver that can be accessed with a browser

Also contains functionality to import from excel, based on a file structure similar to the one in tests + category mapping defined in a file "excel_mapping.json" that can be created by copying the "excel_mapping_template.json"
